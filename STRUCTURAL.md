# Structural coverage

What the client is *proven* to do when the network, the controller or the clock
misbehaves - as opposed to what it does on a good day, which is
[COVERAGE.md](docs/COVERAGE.md) and the README tables.

Regenerated by `npm run structural`. Loop definition:
`docs/tasks/structural-coverage-loop.md`. Machine state:
`docs/structural/state.json`.

**19 verified · 7 failing · 2 untested · 2 manual-only · 0 excluded** (of 30 cells)

Every cell is one scenario on one protocol generation. A cell is `verified`
only when a test asserting it ran green against a live controller; a cell with
no test is `untested`, never assumed.

## Matrix

| Scenario | RWS 1.0 (IRC5) | RWS 2.0 (OmniCore) |
|---|---|---|
| Connection drop mid-request | verified | verified |
| Half-open freeze (sockets alive, no forwarding) | verified | verified |
| Controller warm restart | untested | untested |
| Session idle past expiry, then burst | verified | verified |
| Session-slot pressure over reconnect cycles | **FAILING** | **FAILING** |
| 401 storm / credentials changed mid-session | verified | verified |
| Mastership / write-access conflict | **FAILING** | **FAILING** |
| Rapid connect/disconnect churn (50+ cycles) | **FAILING** | verified |
| Latency injection (500 ms - 5 s) | **FAILING** | **FAILING** |
| 503 storm / backpressure | verified | verified |
| Subscription event flood | verified | verified |
| Malformed / truncated responses | verified | verified |
| Multi-robot fault isolation | verified | verified |
| ConnectionQuality truthfulness (cross-cutting) | verified | verified |
| 24 h soak with periodic injected faults | manual-only | manual-only |

## Cells

### S01-drop-midrequest - Connection drop mid-request

**rws1** - verified (3 passed, 0 failed)

- plain GET mid-flight -> typed RwsError, never an uncaught throw
- mastership-guarded POST mid-flight -> mastership released or reacquirable, no stuck hold
- file transfer mid-flight -> typed error, no partial file silently accepted
- quality reflects the drop (stale or polling), never stays live

**rws2** - verified (3 passed, 0 failed)

- plain GET mid-flight -> typed RwsError, never an uncaught throw
- mastership-guarded POST mid-flight -> write access released or reacquirable
- file transfer mid-flight -> typed error, no partial file silently accepted
- quality reflects the drop (stale or polling), never stays live

### S02-half-open-freeze - Half-open freeze (sockets alive, no forwarding)

**rws1** - verified (4 passed, 0 failed)

- heartbeat detects within 2x ping interval
- frozen socket is terminated, reconnect path runs
- onLost fires at most once if the budget is exhausted
- onRestored fires after recovery

**rws2** - verified (4 passed, 0 failed)

- app-level PING/PONG deadline detects within 2x ping interval
- frozen socket is terminated, re-POST /subscription runs
- onLost fires at most once if the budget is exhausted
- onRestored fires after recovery

### S03-warm-restart - Controller warm restart

**rws1** - untested (0 passed, 0 failed)

- mid-session: silent re-auth, consumer sees no error
- mid-subscription: subscriber re-registers, events resume
- mid-mastership-hold: hold is not falsely believed after restart
- RWS port may DRIFT on IRC5 (new RobVC process) - client or test must rediscover
- OPEN 2026-08-09, next thing to look at: this cell fails with CONTROLLER_BUSY in ~5 s, BEFORE it restarts anything, on a controller that is demonstrably healthy - 5/5 connect/disconnect cycles pass immediately afterwards, and 40/40 pass on a drained one. So it is not the cumulative-load ceiling that explains the other rws1 cells; something in this cell's own setup provokes the 503. The setup is: discoverControllers(), a chaos proxy, a client through it, connect(), getSpeedRatio(). Note the first two awaits after connect() are UNWRAPPED, so a transient 503 there aborts the cell before any restart happens. Start by wrapping those in a retry that treats CONTROLLER_BUSY as 'not ready yet' - which is legitimate here, since the cell asserts restart survival, not the controller's momentary availability. rws2 PASSES this cell.

**rws2** - untested (0 passed, 0 failed)

- mid-session: fresh session cookie adopted (old one is dead)
- mid-subscription: re-POST + WS upgrade with the NEW cookie, not the stale one
- mid-write-access-hold: control-station registration re-established
- quality returns to live/polling, not stuck reconnecting

### S04-idle-expiry - Session idle past expiry, then burst

**rws1** - verified (3 passed, 0 failed)

- silent re-auth on the first request after expiry
- no error surfaced to the consumer
- burst after expiry does not exceed the rate ceiling

**rws2** - verified (3 passed, 0 failed)

- silent re-auth on the first request after expiry
- no error surfaced to the consumer
- burst after expiry does not exceed the rate ceiling

### S05-session-slot-pressure - Session-slot pressure over reconnect cycles

**rws1** - **FAILING** (2 passed, 2 failed)

- repeated connect/disconnect reuses slots - controller session count flat
- disconnect issues GET /logout (clearSession is a deliberate no-op)
- IRC5 caps at 70 sessions; churn must not approach it
- EVIDENCE 2026-08-09, measured OUTSIDE the cell with paired controls. (1) 30 connect/disconnect cycles WITH disconnect all succeed; the same loop WITHOUT disconnect fails at cycle 2 with CONTROLLER_BUSY - so the client frees its slot, and the method would unmistakably detect it if it did not. (2) On a DRAINED controller, 40 connect/disconnect cycles and 40 subscribe/unsubscribe cycles BOTH pass 40/40 - so the RWS 1.0 subscriber releases its controller-side registrations too. No client leak on either path.
- CONCLUSION: the 503s seen in a full pass are CUMULATIVE rig load, not any single cell leaking. Every cell passes alone; fourteen fault-injection cells back-to-back exhaust the IRC5's 70-slot budget faster than its 300 s expiry reclaims it, and once the pool is dry every LATER cell fails CONTROLLER_BUSY for a reason unrelated to what it asserts. Run rws1 cells in batches with a drain between, or restart the IRC5 between groups. This is a property of the controller, and no client change can fix it.

Failures:

- `S05-session-slot-pressure/rws1 > reconnect cycles leave the controller session count flat` - Controller busy (503) - retry later
- `S05-session-slot-pressure/rws1 > RobotManager reconnect cycles stay flat and leave quality honest` - Controller busy (503) - retry later

**rws2** - **FAILING** (2 passed, 4 failed)

- EXCLUDED sub-property: 'controller session count flat' is NOT OBSERVABLE on RWS 2.0. The cell's own premise guard demonstrated it: connecting a client leaves GET /users unchanged (2 -> 2), so that list enumerates users, not sessions. A measurement that cannot see one deliberate session cannot see twenty accidental ones, and the test correctly refuses to assert a leak it cannot detect rather than reporting a false 'flat'.
- credentials are not re-sent once a session cookie is held
- subscription re-POST rides the existing session (no new session per subscribe)
- OPEN: the two properties above need a mock HTTPS server to assert - a TCP chaos proxy cannot read TLS headers, so wire-level claims are unobservable through it on this generation

Failures:

- `S05-session-slot-pressure/rws2 > reconnect cycles leave the controller session count flat` - connecting a client left GET /users unchanged (2 -> 2) - the session count is not observing our sessions, so no leak below could ever fail it: expected 2 to be greater than 2
- `S05-session-slot-pressure/rws2 > does not re-send Basic credentials once a session cookie is held` - connecting a client left GET /users unchanged (2 -> 2) - the session count is not observing our sessions, so no leak below could ever fail it: expected 2 to be greater than 2
- `S05-session-slot-pressure/rws2 > repeated subscribe/unsubscribe rides the existing session` - connecting a client left GET /users unchanged (2 -> 2) - the session count is not observing our sessions, so no leak below could ever fail it: expected 2 to be greater than 2
- `S05-session-slot-pressure/rws2 > RobotManager reconnect cycles stay flat and leave quality honest` - the manager's own session is invisible to GET /users (2 -> 2) - a per-cycle leak would be invisible too: expected 2 to be greater than 2

### S06-401-storm - 401 storm / credentials changed mid-session

**rws1** - verified (5 passed, 0 failed)

- typed AUTH_FAILED, not a generic error
- no infinite retry loop
- clean disconnect; quality -> disconnected

**rws2** - verified (5 passed, 0 failed)

- typed AUTH_FAILED, not a generic error
- no infinite retry loop
- WS upgrade 401 does not spin forever (live-observed failure mode on RW7.21)

### S07-mastership-conflict - Mastership / write-access conflict

**rws1** - **FAILING** (2 passed, 1 failed)

- second holder -> typed MASTERSHIP_REQUIRED
- recovery after the other side releases
- release-on-disconnect actually releases

Failures:

- `S07-mastership-conflict/rws1 > quality stays truthful while only WRITE access is contended (S14 cross-cutting)` - Controller busy (503) - retry later

**rws2** - **FAILING** (3 passed, 1 failed)

- second holder -> typed MASTERSHIP_REQUIRED
- RW8 control-station path: conflict surfaces typed, not as a raw 403
- release-on-disconnect actually releases

Failures:

- `S07-mastership-conflict/rws2 > RW8 control-station conflict surfaces a typed error, not a raw 403` - Permission denied: HTTP 403 from POST /rw/controlstation/writeaccess/request - request remote-modify privilege (RMMP) via POST /users/rmmp and approve the popup on the FlexPendant, or add the required UAS grant (e.g. "Remote Start and Stop in Auto") in RobotStudio > UAS (Remote Control Station cannot take SPoC when it is taken. code:-1073435870 icode:-20109)

### S08-churn - Rapid connect/disconnect churn (50+ cycles)

**rws1** - **FAILING** (1 passed, 2 failed)

- zero leaked sockets
- zero leaked timers
- zero leaked sessions
- zero leaked subscriptions
- heap does not grow monotonically

Failures:

- `S08-churn/rws1 > 50 connect/disconnect cycles leak no sockets, timers or controller sessions` - Hook timed out in 10000ms. If this is a long-running hook, pass a timeout value as the last argument or configure it globally with "hookTimeout".
- `S08-churn/rws1 > 50 subscribe/unsubscribe cycles leak no WebSockets, heartbeat timers or subscription groups` - Controller busy (503) - retry later

**rws2** - verified (3 passed, 0 failed)

- zero leaked sockets
- zero leaked timers
- zero leaked sessions
- zero leaked subscriptions
- heap does not grow monotonically

### S09-latency - Latency injection (500 ms - 5 s)

**rws1** - **FAILING** (0 passed, 5 failed)

- configured timeout fires, typed error
- request queue drains, no pile-up
- rate limiter still respected under delay

Failures:

- `S09-latency/rws1 > the configured timeout fires under latency and surfaces as a typed RwsError` - Controller busy (503) - retry later
- `S09-latency/rws1 > every request issued under latency settles - the queue drains, nothing piles up` - Controller busy (503) - retry later
- `S09-latency/rws1 > a batch of timeouts does not wedge the requests queued behind them` - Controller busy (503) - retry later
- `S09-latency/rws1 > the >=55 ms pacing floor still holds under injected latency` - Controller busy (503) - retry later
- `S09-latency/rws1 > quality does not claim "disconnected" while slow requests still succeed (S14 cross-cutting)` - Controller busy (503) - retry later

**rws2** - **FAILING** (0 passed, 5 failed)

- configured timeout fires, typed error
- request queue drains, no pile-up
- rate limiter still respected under delay

Failures:

- `S09-latency/rws2 > the configured timeout fires under latency and surfaces as a typed RwsError` - HTTP 503 from GET /rw/system
- `S09-latency/rws2 > every request issued under latency settles - the queue drains, nothing piles up` - HTTP 503 from GET /rw/system
- `S09-latency/rws2 > a batch of timeouts does not wedge the requests queued behind them` - HTTP 503 from GET /rw/system
- `S09-latency/rws2 > the >=55 ms pacing floor still holds under injected latency` - HTTP 503 from GET /rw/system
- `S09-latency/rws2 > quality does not claim "disconnected" while slow requests still succeed (S14 cross-cutting)` - HTTP 503 from GET /rw/system

### S10-503-storm - 503 storm / backpressure

**rws1** - verified (5 passed, 0 failed)

- CONTROLLER_BUSY typed
- backpressure holds under the <20 req/s ceiling
- requests eventually succeed, no starvation

**rws2** - verified (5 passed, 0 failed)

- CONTROLLER_BUSY typed
- backpressure holds under the <20 req/s ceiling
- requests eventually succeed, no starvation

### S11-event-flood - Subscription event flood

**rws1** - verified (4 passed, 0 failed)

- no unbounded buffering
- events delivered in order
- no silent drops - any drop is signalled

**rws2** - verified (4 passed, 0 failed)

- no unbounded buffering
- events delivered in order
- no silent drops - any drop is signalled

### S12-malformed - Malformed / truncated responses

**rws1** - verified (5 passed, 0 failed)

- parsers never throw uncaught
- every failure is a typed RwsError (PARSE_ERROR family)
- truncated body mid-element does not hang the request

**rws2** - verified (5 passed, 0 failed)

- parsers never throw uncaught (XHTML and hal+json paths)
- every failure is a typed RwsError (PARSE_ERROR family)
- truncated body mid-element does not hang the request

### S13-multi-robot-isolation - Multi-robot fault isolation

**rws1** - verified (2 passed, 0 failed)

- one manager's failure never changes another's state
- quality is per-robot, not global
- change notifications carry the robot id

**rws2** - verified (3 passed, 0 failed)

- one manager's failure never changes another's state
- quality is per-robot, not global
- mixed-generation fleet isolates too (RW6 + RW7 in one MultiRobotManager)

### S14-quality-truthfulness - ConnectionQuality truthfulness (cross-cutting)

**rws1** - verified (3 passed, 0 failed)

- asserted inside every other cell, not as a separate run
- quality never claims live while the stream is down
- quality never claims disconnected while requests still succeed
- every transition carries a non-empty human-readable reason

**rws2** - verified (3 passed, 0 failed)

- asserted inside every other cell, not as a separate run
- quality never claims live while the stream is down
- quality never claims disconnected while requests still succeed
- every transition carries a non-empty human-readable reason

### S15-soak - 24 h soak with periodic injected faults

**rws1** - manual-only

- RSS drift within threshold
- handle/socket count flat
- zero uncaught exceptions
- final state matches controller truth

> Occupies the machine ~24 h; scheduling is a human checkpoint per the task doc.

**Manual procedure**

1. Restart both VCs so no session backlog is carried in.
2. `node scripts/soak.mjs --minutes 1440` (add `--rss-drift-mb` / `--handle-growth` to override the thresholds).
3. Leave the machine otherwise idle - the run holds a manager against each generation and injects a fault every 15 s.

Thresholds are fixed BEFORE the run, in the script, so the verdict cannot be rationalised afterwards:
- **RSS drift** <= 75 MB from a POST-WARMUP baseline (warm-up excluded because the first minute is lazy imports and JIT, which never recur).
- **Handle growth** <= 5 for any single active-handle kind. Sockets and timers are what leak here; one socket in TIME_WAIT is noise, sustained growth is not.
- **Uncaught exceptions** exactly 0. Any unhandled rejection fails the soak outright regardless of memory.
- **Final state** must match an INDEPENDENT client's reading, not the manager's own cached view.

Output lands in `docs/structural/soak-<timestamp>.json`. A scaled run is written with `scaled: true` and is evidence the harness works, NOT the acceptance run.

**rws2** - manual-only

- RSS drift within threshold
- handle/socket count flat
- zero uncaught exceptions
- final state matches controller truth

> Occupies the machine ~24 h; scheduling is a human checkpoint per the task doc.

**Manual procedure**

1. Restart both VCs so no session backlog is carried in.
2. `node scripts/soak.mjs --minutes 1440` (add `--rss-drift-mb` / `--handle-growth` to override the thresholds).
3. Leave the machine otherwise idle - the run holds a manager against each generation and injects a fault every 15 s.

Thresholds are fixed BEFORE the run, in the script, so the verdict cannot be rationalised afterwards:
- **RSS drift** <= 75 MB from a POST-WARMUP baseline (warm-up excluded because the first minute is lazy imports and JIT, which never recur).
- **Handle growth** <= 5 for any single active-handle kind. Sockets and timers are what leak here; one socket in TIME_WAIT is noise, sustained growth is not.
- **Uncaught exceptions** exactly 0. Any unhandled rejection fails the soak outright regardless of memory.
- **Final state** must match an INDEPENDENT client's reading, not the manager's own cached view.

Output lands in `docs/structural/soak-<timestamp>.json`. A scaled run is written with `scaled: true` and is evidence the harness works, NOT the acceptance run.

## Running the loop

```bash
npm run structural          # run the auto cells, regenerate this file + state.json
npm run structural -- --no-run   # re-map the LAST run's results (after editing the matrix)
npm run structural -- --strict   # non-zero exit while any cell is failing or untested
node scripts/soak.mjs --minutes 1440   # S15 acceptance run (see below)
```

Cells run **serially** (`--no-file-parallelism`). Vitest parallelises files by
default, which would put several live clients on one controller at once; each
paces itself correctly but nothing coordinates across clients, and the pair
exceeds the controller's <20 req/s ceiling.

**Start from a freshly restarted controller, and expect one pass not to fit.**

The IRC5 caps at 70 sessions and reclaims them on a 300 s timer. Fourteen
fault-injection cells back-to-back consume that budget faster than it is
reclaimed, and once the pool is dry every LATER cell fails CONTROLLER_BUSY for a
reason unrelated to what it asserts. This is a property of the controller: it is
not a client defect, and no client change fixes it.

Measured 2026-08-09, with the controls that rule out a leak:

| Loop, on a drained IRC5 | Result |
|---|---|
| 40 connect/disconnect cycles | 40/40 pass |
| 40 subscribe/unsubscribe cycles | 40/40 pass |
| connect/disconnect WITHOUT disconnect | fails at cycle **2** |

So both paths release what they take, and the method would catch it if they did
not. Every cell also passes on its own. What does not fit is the SUM.

Practically: run the rws1 cells in two or three groups with `--cooldown`, letting
the controller drain between groups, or restart the IRC5 between them. A single
uninterrupted pass over all fourteen cells will show phantom 503s in whichever
cells happen to run last.

## Known limits

- Direct session counting is **not observable on RWS 2.0**: `GET /users`
  enumerates users, not sessions, so it does not move when a client connects.
  S05's premise guard detects this and refuses to assert a leak it cannot see.
- A half-open state affecting **only** the RWS 2.0 subscription WebSocket, while
  HTTP still works, cannot be detected in band: the controller rejects client
  frames on that socket and an idle subscription sends nothing back. Detection
  covers link-level failure.
- TLS is opaque to the TCP chaos proxy, so wire-level assertions (headers,
  request lines) are only possible on RWS 1.0 or against a mock HTTPS server.
- Live cells run against **localhost virtual controllers only**; the helper in
  `tests/helpers/liveControllers.ts` refuses any other host outright.
- VC RWS ports drift across restarts, so they are discovered every run rather
  than configured. Override with `RWS_TEST_PORT_RW6` / `RWS_TEST_PORT_RW7`.
- Warm restart is the destructive ceiling. No cell resets a system, restores a
  backup, runs an installer, or mutates UAS.
- IK/FK failures on a VC are a missing option (PC Interface 616-1), not a defect.
